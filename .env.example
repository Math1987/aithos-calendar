# Optional local credentials only. Prefer AWS_PROFILE=aithos-prod after SSO login.
AWS_ACCESS_KEY_ID=example
AWS_SECRET_ACCESS_KEY=example
AWS_SESSION_TOKEN=example
AWS_DEFAULT_REGION=eu-west-3
GH_TOKEN=example

# Booking adapter: server-side only; never expose these in the website.
ANAKIN_API_KEY=example
# Attendee name/email come from the visitor booking page.
# Missing or ambiguous fields are requested by the controlled booking command.

# Google sign-in: the client secret stays on the server.
GOOGLE_OAUTH_CLIENT_ID=example.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=example
GOOGLE_OAUTH_REDIRECT_URI=https://api.calendar.aithos.world/auth/google/callback
